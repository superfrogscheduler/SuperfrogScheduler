export interface User {
    id?: string,
    email?: string,
    first_name?: string,
    last_name?: string,
    is_active?: string,
    is_staff?: string,
    is_admin?: string,
    created_at?: string,
    updated_at?: string,
    phone?: string;
}