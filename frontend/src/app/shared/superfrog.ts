export interface Superfrog {
    id?: number,
    email?: string,
    first_name?: string,
    last_name?: string,
    phone?: string;
    created_at?: string,
    updated_at?: string,
    is_active?: boolean,
    is_staff?: boolean,
    is_admin?: boolean,
}