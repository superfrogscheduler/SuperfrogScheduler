export interface Superfrog {
    id?: string,
    email?: string,
    first_name?: string,
    last_name?: string,
    phone?: string;
    is_active?: boolean,
    is_staff?: boolean,
    is_admin?: boolean,
}